
# 105_手動ログ記録機能実装手順

このセクションでは、ホーム画面から手動でログを記録するための機能の実装手順を説明します。これにより、ユーザーはワンタップ記録だけでなく、時刻やメモを指定して詳細なログを残せるようになります。

---

## 1. フローティングアクションボタン (`QuickAddButton.tsx`) の作成

### 1.1. コンポーネントファイルの作成

`src/components/QuickAddButton.tsx` を作成します。

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/components/QuickAddButton.tsx
'use client';

import React from 'react';

interface QuickAddButtonProps {
  onClick: () => void;
}

export const QuickAddButton: React.FC<QuickAddButtonProps> = ({ onClick }) => {
  return (
    <button
      onClick={onClick}
      className="fixed bottom-20 right-4 bg-blue-600 text-white rounded-full w-14 h-14 flex items-center justify-center shadow-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 transition-all duration-200 ease-in-out transform hover:scale-105"
    >
      <span className="text-3xl font-light">+</span>
    </button>
  );
};
```

</details>

### 1.2. 実装のポイント

-   画面右下に固定表示される円形のボタンです。
-   `onClick` プロパティを受け取り、クリック時に親コンポーネントの関数を実行します。

---

## 2. 手動ログ追加モーダル (`ManualAddLogModal.tsx`) の作成

### 2.1. コンポーネントファイルの作成

`src/components/ManualAddLogModal.tsx` を作成します。

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/components/ManualAddLogModal.tsx
'use client';

import React, { useState, useEffect } from 'react';
import { useLogbook } from '@/hooks/useLogbook';

interface ManualAddLogModalProps {
  isOpen: boolean;
  onClose: () => void;
}

export const ManualAddLogModal: React.FC<ManualAddLogModalProps> = ({ isOpen, onClose }) => {
  const { tasks, addLog, loading: logbookLoading } = useLogbook();
  const [selectedTask, setSelectedTask] = useState<string>('');
  const [logTime, setLogTime] = useState<string>('');
  const [note, setNote] = useState<string>('');
  const [isAdding, setIsAdding] = useState(false);

  useEffect(() => {
    if (isOpen) {
      // モーダルが開いたときに現在時刻をセット
      const now = new Date();
      const hours = String(now.getHours()).padStart(2, '0');
      const minutes = String(now.getMinutes()).padStart(2, '0');
      setLogTime(`${hours}:${minutes}`);

      // タスクが読み込まれていれば最初のタスクをデフォルト選択
      if (tasks.length > 0) {
        setSelectedTask(tasks[0].id);
      }
    }
  }, [isOpen, tasks]);

  const handleSubmit = async () => {
    if (!selectedTask || !logTime) {
      alert('タスクと時刻を入力してください。');
      return;
    }

    setIsAdding(true);
    try {
      const task = tasks.find(t => t.id === selectedTask);
      if (task) {
        await addLog(task, logTime, note); // logTimeとnoteを渡す
        alert('ログを追加しました！');
        onClose(); // モーダルを閉じる
        // フォームをリセット
        setNote('');
        setLogTime('');
        setSelectedTask('');
      }
    } catch (error) {
      console.error('ログの追加に失敗しました:', error);
      alert('ログの追加に失敗しました。');
    } finally {
      setIsAdding(false);
    }
  };

  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
      <div className="bg-white p-6 rounded-lg shadow-xl w-full max-w-md mx-4">
        <h2 className="text-2xl font-bold mb-4">手動でログを追加</h2>

        <div className="mb-4">
          <label htmlFor="logTime" className="block text-gray-700 text-sm font-bold mb-2">時刻</label>
          <input
            type="time"
            id="logTime"
            value={logTime}
            onChange={(e) => setLogTime(e.target.value)}
            className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
          />
        </div>

        <div className="mb-4">
          <label htmlFor="taskSelect" className="block text-gray-700 text-sm font-bold mb-2">タスク</label>
          {logbookLoading ? (
            <p>タスク読み込み中...</p>
          ) : tasks.length > 0 ? (
            <select
              id="taskSelect"
              value={selectedTask}
              onChange={(e) => setSelectedTask(e.target.value)}
              className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
            >
              {tasks.map((task) => (
                <option key={task.id} value={task.id}>
                  {task.name}
                </option>
              ))}
            </select>
          ) : (
            <p>タスクがありません。設定から追加してください。</p>
          )}
        </div>

        <div className="mb-6">
          <label htmlFor="note" className="block text-gray-700 text-sm font-bold mb-2">メモ (任意)</label>
          <textarea
            id="note"
            value={note}
            onChange={(e) => setNote(e.target.value)}
            rows={3}
            className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
            placeholder="例: ご飯を完食しました"
          ></textarea>
        </div>

        <div className="flex justify-end gap-2">
          <button
            onClick={onClose}
            className="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
            disabled={isAdding}
          >
            キャンセル
          </button>
          <button
            onClick={handleSubmit}
            className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
            disabled={isAdding || logbookLoading}
          >
            {isAdding ? '追加中...' : 'ログを追加'}
          </button>
        </div>
      </div>
    </div>
  );
};
```

</details>

### 2.2. 実装のポイント

-   モーダルの表示/非表示は `isOpen` プロパティで制御され、`onClose` で閉じられます。
-   時刻入力 (`<input type="time">`)、タスク選択 (`<select>`)、メモ入力 (`<textarea>`) のフォーム要素が含まれます。
-   `useEffect` を使用して、モーダルが開いた際に現在時刻をデフォルトで設定し、タスクが読み込まれていれば最初のタスクをデフォルト選択します。
-   `useLogbook` フックの `addLog` 関数を呼び出し、選択されたタスク、時刻、メモを渡してログを記録します。
-   ログ記録中はボタンを無効化し、ローディング状態を表示します。

---

## 3. `useLogbook` フックの拡張

手動ログ記録に対応するため、`src/hooks/useLogbook.ts` の `addLog` 関数を拡張します。

### 3.1. `addLog` 関数の変更

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/hooks/useLogbook.ts
import { useState, useEffect } from 'react';
import {
  collection,
  query,
  onSnapshot,
  addDoc,
  serverTimestamp,
  orderBy,
  Timestamp,
  where,
} from 'firebase/firestore';
import { db } from '@/lib/firebase';
import { useAuth } from './useAuth';
import { usePets } from './usePets';

export interface Task {
  id: string;
  name: string;
  color: string;
  order: number;
}

export interface Log {
  id: string;
  taskName: string;
  taskId: string;
  timestamp: Timestamp;
  note?: string; // ★追加：メモ
}

export const useLogbook = () => {
  const { user } = useAuth();
  const { selectedPetId } = usePets();
  const [tasks, setTasks] = useState<Task[]>([]);
  const [logs, setLogs] = useState<Log[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    // ... (タスクとログの監視ロジック) ...
  }, [user, selectedPetId]);

  const addLog = async (task: Task, logTime?: string, note?: string) => {
    if (!user) {
      alert('ログインが必要です。');
      return;
    }
    if (!selectedPetId) {
      alert('ペットが選択されていません。');
      return;
    }

    try {
      const logsCollection = collection(db, 'dogs', selectedPetId, 'logs');

      let timestampToUse;
      if (logTime) {
        const [hours, minutes] = logTime.split(':').map(Number);
        const now = new Date();
        now.setHours(hours, minutes, 0, 0);
        timestampToUse = now;
      } else {
        timestampToUse = serverTimestamp();
      }

      await addDoc(logsCollection, {
        taskName: task.name,
        taskId: task.id,
        timestamp: timestampToUse,
        inputType: logTime ? 'manual' : 'auto',
        note: note || '',
        createdBy: user.uid,
        createdAt: serverTimestamp(),
        updatedAt: serverTimestamp(),
      });
    } catch (error) {
      console.error('ログの記録に失敗しました:', error);
      alert('ログの記録に失敗しました。');
    }
  };

  return { tasks, logs, loading, addLog };
};
```

</details>

### 3.2. 実装のポイント

-   `addLog` 関数が `logTime` (文字列 `HH:mm`) と `note` (文字列) をオプション引数として受け取るように変更しました。
-   `logTime` が指定された場合、それを `Date` オブジェクトに変換し、`timestamp` フィールドとして使用します。指定されない場合は `serverTimestamp()` を使用します。
-   `inputType` フィールドを `manual` または `auto` に設定します。
-   `note` フィールドをFirestoreに保存します。
-   `Log` インターフェースに `note?: string;` を追加し、型安全性を確保しました。

---

## 4. メインページ (`page.tsx`) への統合

作成した `<QuickAddButton />` と `<ManualAddLogModal />` を `src/app/page.tsx` に組み込みます。

### 4.1. `page.tsx` の修正

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/app/page.tsx
'use client';

import { useState } from 'react';
import { useAuth } from '@/hooks/useAuth';
import LoginButton from '@/components/LoginButton';
import UserProfile from '@/components/UserProfile';
import { TaskSelector } from '@/components/TaskSelector';
import { LogTimeline } from '@/components/LogTimeline';
import { Header } from '@/components/Header';
import { FooterNav } from '@/components/FooterNav';
import { QuickAddButton } from '@/components/QuickAddButton';
import { ManualAddLogModal } from '@/components/ManualAddLogModal';

export default function Home() {
  const { user, loading } = useAuth();
  const [isModalOpen, setIsModalOpen] = useState(false);

  if (loading) {
    return (
      <div className="min-h-screen flex items-center justify-center">
        <p>読み込み中...</p>
      </div>
    );
  }

  return (
    <div className="flex flex-col min-h-screen bg-gray-800">
      <Header />
      <main className="flex-grow w-full pb-16">
        {user ? (
          <>
            <div className="max-w-lg mx-auto p-4 space-y-4">
              <UserProfile />
              <TaskSelector />
              <LogTimeline />
            </div>
            <QuickAddButton onClick={() => setIsModalOpen(true)} />
            <ManualAddLogModal
              isOpen={isModalOpen}
              onClose={() => setIsModalOpen(false)}
            />
          </>
        ) : (
          <div className="min-h-full flex flex-col items-center justify-center">
            <h1 className="text-3xl font-bold mb-8">aina Life アプリ</h1>
            <LoginButton />
          </div>
        )}
      </main>
      <FooterNav />
    </div>
  );
}
```

</details>

### 4.2. 実装のポイント

-   `isModalOpen` ステートでモーダルの表示/非表示を制御します。
-   `QuickAddButton` の `onClick` で `isModalOpen` を `true` に設定し、モーダルを開きます。
-   `ManualAddLogModal` の `onClose` で `isModalOpen` を `false` に設定し、モーダルを閉じます。

---

## 5. 動作確認

開発サーバーを起動し、ブラウザでアプリケーションにアクセスして、以下の点を確認してください。

-   ホーム画面右下に「＋」ボタンが表示されること。
-   「＋」ボタンをクリックすると、手動ログ追加モーダルが表示されること。
-   モーダル内で時刻、タスク、メモを入力し、「ログを追加」ボタンをクリックすると、ログがFirestoreに記録され、タイムラインに表示されること。
-   「キャンセル」ボタンをクリックすると、モーダルが閉じられること。
