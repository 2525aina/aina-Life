# 106_履歴画面実装手順

このセクションでは、犬の行動ログを日付ごとに閲覧・管理できる履歴画面の実装手順を説明します。これにより、ユーザーは過去の記録を簡単に確認し、必要に応じて編集・削除できるようになります。

---

## 1. `useLogbook` フックの拡張 (日付指定とログ編集・削除)

### 1.1. `useLogbook` の `targetDate` パラメータ対応

`src/hooks/useLogbook.ts` を修正し、オプションの `targetDate` パラメータを受け取れるようにします。これにより、特定の日付のログを取得できるようになります。

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/hooks/useLogbook.ts
import { useState, useEffect } from 'react';
import {
  collection,
  query,
  onSnapshot,
  addDoc,
  serverTimestamp,
  orderBy,
  Timestamp,
  where,
  doc,
  updateDoc,
  deleteDoc,
} from 'firebase/firestore';
import { db } from '@/lib/firebase';
import { useAuth } from './useAuth';
import { usePets } from './usePets';

export interface Task {
  id: string;
  name: string;
  color: string;
  order: number;
}

export interface Log {
  id: string;
  taskName: string;
  taskId: string;
  timestamp: Timestamp;
  note?: string;
}

export const useLogbook = (targetDate?: Date) => {
  const { user } = useAuth();
  const { selectedPetId } = usePets();
  const [tasks, setTasks] = useState<Task[]>([]);
  const [logs, setLogs] = useState<Log[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    if (!user || !selectedPetId) {
      setTasks([]);
      setLogs([]);
      setLoading(false);
      return;
    }

    // 1. タスクの監視 (変更なし)
    const tasksCollection = collection(db, 'dogs', selectedPetId, 'tasks');
    const tasksQuery = query(tasksCollection, orderBy('order'));
    const unsubscribeTasks = onSnapshot(
      tasksQuery,
      (snapshot) => {
        const fetchedTasks = snapshot.docs.map((doc) => ({
          id: doc.id,
          ...(doc.data() as Omit<Task, 'id'>),
        }));
        setTasks(fetchedTasks);
        setLoading(false);
      },
      (error) => {
        console.error('タスクの取得に失敗しました:', error);
        setLoading(false);
      }
    );

    // 2. ログの監視 (targetDateを使用)
    const logsCollection = collection(db, 'dogs', selectedPetId, 'logs');

    const dateToFetch = targetDate || new Date();

    const startOfDay = new Date(dateToFetch);
    startOfDay.setHours(0, 0, 0, 0);
    const endOfDay = new Date(dateToFetch);
    endOfDay.setHours(23, 59, 59, 999);

    const logsQuery = query(
      logsCollection,
      where('timestamp', '>=', startOfDay),
      where('timestamp', '<', endOfDay),
      orderBy('timestamp', 'desc')
    );

    const unsubscribeLogs = onSnapshot(logsQuery, (snapshot) => {
      const fetchedLogs = snapshot.docs.map((doc) => ({
        id: doc.id,
        ...(doc.data() as Omit<Log, 'id'>),
      }));
      setLogs(fetchedLogs);
    });

    return () => {
      unsubscribeTasks();
      unsubscribeLogs();
    };
  }, [user, selectedPetId, targetDate]);

  // addLog関数 (変更なし)
  const addLog = async (task: Task, logTime?: string, note?: string) => {
    if (!user || !selectedPetId) {
      alert('ログインが必要です。');
      return;
    }
    try {
      const logsCollection = collection(db, 'dogs', selectedPetId, 'logs');
      let timestampToUse;
      if (logTime) {
        const [hours, minutes] = logTime.split(':').map(Number);
        const now = new Date();
        now.setHours(hours, minutes, 0, 0);
        timestampToUse = now;
      } else {
        timestampToUse = serverTimestamp();
      }
      await addDoc(logsCollection, {
        taskName: task.name,
        taskId: task.id,
        timestamp: timestampToUse,
        inputType: logTime ? 'manual' : 'auto',
        note: note || '',
        createdBy: user.uid,
        createdAt: serverTimestamp(),
        updatedAt: serverTimestamp(),
      });
    } catch (error) {
      console.error('ログの記録に失敗しました:', error);
      alert('ログの記録に失敗しました。');
    }
  };

  // ログを更新する関数
  const updateLog = async (logId: string, updatedData: Partial<Log>) => {
    if (!user || !selectedPetId) {
      alert('ログインが必要です。');
      return;
    }
    try {
      const logRef = doc(db, 'dogs', selectedPetId, 'logs', logId);
      await updateDoc(logRef, {
        ...updatedData,
        updatedAt: serverTimestamp(),
      });
      console.log('ログを更新しました:', logId);
    } catch (error) {
      console.error('ログの更新に失敗しました:', error);
      alert('ログの更新に失敗しました。');
    }
  };

  // ログを削除する関数
  const deleteLog = async (logId: string) => {
    if (!user || !selectedPetId) {
      alert('ログインが必要です。');
      return;
    }
    if (!confirm('本当にこのログを削除しますか？')) {
      return;
    }
    try {
      const logRef = doc(db, 'dogs', selectedPetId, 'logs', logId);
      await deleteDoc(logRef);
      console.log('ログを削除しました:', logId);
    } catch (error) {
      console.error('ログの削除に失敗しました:', error);
      alert('ログの削除に失敗しました。');
    }
  };

  return { tasks, logs, loading, addLog, updateLog, deleteLog };
};
```

</details>

### 1.2. 実装のポイント

-   `useLogbook` フックが `targetDate?: Date` を引数として受け取るように変更しました。
-   `useEffect` 内のログ取得クエリで、`targetDate` が指定されていればその日付の0時0分から23時59分59秒までのログを取得するようにしました。
-   `targetDate` が指定されない場合は、これまで通り今日の日付のログを取得します。
-   `updateLog` と `deleteLog` 関数を追加し、Firestoreのログを更新・削除できるようにしました。

---

## 2. `LogTimeline` コンポーネントの汎用化と編集・削除ボタンの追加

### 2.1. `LogTimeline` の汎用化

`src/components/LogTimeline.tsx` を修正し、ログデータをプロパティとして受け取るように汎用化します。これにより、ホーム画面と履歴画面の両方で再利用可能になります。

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/components/LogTimeline.tsx
'use client';

import React, { useState } from 'react';
import { Timestamp } from 'firebase/firestore';
import { Log, useLogbook } from '@/hooks/useLogbook';
import { EditLogModal } from '@/components/EditLogModal';

// Timestampオブジェクトを "HH:mm" 形式の文字列にフォーマットするヘルパー関数
const formatTime = (timestamp: Timestamp | null): string => {
  if (!timestamp) {
    return '--:--';
  }
  return timestamp.toDate().toLocaleTimeString('ja-JP', {
    hour: '2-digit',
    minute: '2-digit',
  });
};

interface LogTimelineProps {
  logs: Log[];
  title?: string;
  emptyMessage?: string;
}

export const LogTimeline: React.FC<LogTimelineProps> = ({
  logs,
  title = '記録',
  emptyMessage = 'まだ記録がありません。',
}) => {
  const { updateLog, deleteLog } = useLogbook();
  const [isEditModalOpen, setIsEditModalOpen] = useState(false);
  const [logToEdit, setLogToEdit] = useState<Log | null>(null);

  const handleEdit = (log: Log) => {
    setLogToEdit(log);
    setIsEditModalOpen(true);
  };

  const handleDelete = async (logId: string) => {
    await deleteLog(logId);
  };

  if (logs.length === 0) {
    return (
      <div className="mt-8 text-center text-gray-500">
        <p>{emptyMessage}</p>
      </div>
    );
  }

  return (
    <div className="mt-6 w-full">
      <h2 className="text-lg font-semibold mb-3 text-center border-b pb-2 text-white">
        {title}
      </h2>
      <ul className="space-y-2">
        {logs.map((log) => (
          <li
            key={log.id}
            className="flex items-center p-3 bg-white rounded-lg shadow-sm border border-gray-100 hover:bg-gray-50 transition-colors"
          >
            <span className="font-mono text-base text-gray-700 bg-gray-100 px-2 py-1 rounded">
              {formatTime(log.timestamp)}
            </span>
            <span className="ml-4 font-medium text-gray-800 text-base">
              {log.taskName}
            </span>
            {log.note && ( // メモがあれば表示
              <span className="ml-2 text-sm text-gray-500">
                ({log.note})
              </span>
            )}
            <div className="ml-auto flex space-x-2">
              <button
                onClick={() => handleEdit(log)}
                className="text-blue-500 hover:text-blue-700 text-sm"
              >
                編集
              </button>
              <button
                onClick={() => handleDelete(log.id)}
                className="text-red-500 hover:text-red-700 text-sm"
              >
                削除
              </button>
            </div>
          </li>
        ))}
      </ul>

      <EditLogModal
        isOpen={isEditModalOpen}
        onClose={() => setIsEditModalOpen(false)}
        logToEdit={logToEdit}
      />
    </div>
  );
};
```

</details>

### 2.2. 実装のポイント

-   `LogTimelineProps` インターフェースに `logs`、`title`、`emptyMessage` プロパティを追加しました。
-   `useLogbook` から `updateLog` と `deleteLog` 関数を取得し、ログアイテムの編集・削除ボタンに紐付けました。
-   編集ボタンクリック時に `EditLogModal` を表示し、編集対象のログデータを渡すようにしました。
-   ログにメモがあれば表示するようにしました。

---

## 3. ログ編集モーダル (`EditLogModal.tsx`) の作成

### 3.1. コンポーネントファイルの作成

`src/components/EditLogModal.tsx` を作成します。

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/components/EditLogModal.tsx
'use client';

import React, { useState, useEffect } from 'react';
import { useLogbook, Log, Task } from '@/hooks/useLogbook';
import { Timestamp } from 'firebase/firestore';

interface EditLogModalProps {
  isOpen: boolean;
  onClose: () => void;
  logToEdit: Log | null;
}

export const EditLogModal: React.FC<EditLogModalProps> = ({ isOpen, onClose, logToEdit }) => {
  const { tasks, updateLog, loading: logbookLoading } = useLogbook();
  const [selectedTask, setSelectedTask] = useState<string>('');
  const [logTime, setLogTime] = useState<string>('');
  const [note, setNote] = useState<string>('');
  const [isUpdating, setIsUpdating] = useState(false);

  useEffect(() => {
    if (isOpen && logToEdit) {
      setSelectedTask(logToEdit.taskId);
      const date = logToEdit.timestamp instanceof Timestamp ? logToEdit.timestamp.toDate() : new Date();
      const hours = String(date.getHours()).padStart(2, '0');
      const minutes = String(date.getMinutes()).padStart(2, '0');
      setLogTime(`${hours}:${minutes}`);
      setNote(logToEdit.note || '');
    } else if (isOpen && !logToEdit) {
      const now = new Date();
      const hours = String(now.getHours()).padStart(2, '0');
      const minutes = String(now.getMinutes()).padStart(2, '0');
      setLogTime(`${hours}:${minutes}`);
      if (tasks.length > 0) {
        setSelectedTask(tasks[0].id);
      }
      setNote('');
    }
  }, [isOpen, logToEdit, tasks]);

  const handleSubmit = async () => {
    if (!logToEdit || !selectedTask || !logTime) {
      alert('タスクと時刻を入力してください。');
      return;
    }

    setIsUpdating(true);
    try {
      const task = tasks.find(t => t.id === selectedTask);
      if (task) {
        const [hours, minutes] = logTime.split(':').map(Number);
        const updatedTimestamp = new Date(logToEdit.timestamp instanceof Timestamp ? logToEdit.timestamp.toDate() : new Date());
        updatedTimestamp.setHours(hours, minutes, 0, 0);

        await updateLog(logToEdit.id, {
          taskName: task.name,
          taskId: task.id,
          timestamp: updatedTimestamp,
          note: note,
          updatedAt: new Date(),
        });
        alert('ログを更新しました！');
        onClose();
      }
    } catch (error) {
      console.error('ログの更新に失敗しました:', error);
      alert('ログの更新に失敗しました。');
    }
  };

  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
      <div className="bg-white p-6 rounded-lg shadow-xl w-full max-w-md mx-4">
        <h2 className="text-2xl font-bold mb-4">ログを編集</h2>

        <div className="mb-4">
          <label htmlFor="logTime" className="block text-gray-700 text-sm font-bold mb-2">時刻</label>
          <input
            type="time"
            id="logTime"
            value={logTime}
            onChange={(e) => setLogTime(e.target.value)}
            className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
          />
        </div>

        <div className="mb-4">
          <label htmlFor="taskSelect" className="block text-gray-700 text-sm font-bold mb-2">タスク</label>
          {logbookLoading ? (
            <p>タスク読み込み中...</p>
          ) : tasks.length > 0 ? (
            <select
              id="taskSelect"
              value={selectedTask}
              onChange={(e) => setSelectedTask(e.target.value)}
              className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
            >
              {tasks.map((task) => (
                <option key={task.id} value={task.id}>
                  {task.name}
                </option>
              ))}
            </select>
          ) : (
            <p>タスクがありません。設定から追加してください。</p>
          )}
        </div>

        <div className="mb-6">
          <label htmlFor="note" className="block text-gray-700 text-sm font-bold mb-2">メモ (任意)</label>
          <textarea
            id="note"
            value={note}
            onChange={(e) => setNote(e.target.value)}
            rows={3}
            className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
            placeholder="例: ご飯を完食しました"
          ></textarea>
        </div>

        <div className="flex justify-end gap-2">
          <button
            onClick={onClose}
            className="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
            disabled={isUpdating}
          >
            キャンセル
          </button>
          <button
            onClick={handleSubmit}
            className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
            disabled={isUpdating || logbookLoading}
          >
            {isUpdating ? '更新中...' : 'ログを更新'}
          </button>
        </div>
      </div>
    </div>
  );
};
```

</details>

### 3.2. 実装のポイント

-   `EditLogModalProps` インターフェースに `logToEdit` プロパティを追加し、編集対象のログデータを受け取ります。
-   `useEffect` を使用して、モーダルが開いた際に `logToEdit` のデータでフォームを初期化します。
-   `handleSubmit` 関数内で `useLogbook` の `updateLog` 関数を呼び出し、編集されたログデータをFirestoreに保存します。

---

## 4. メインページ (`page.tsx`) の修正 (LogTimelineへのlogsプロパティ渡し)

### 4.1. `page.tsx` の修正

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/app/page.tsx
'use client';

import { useState } from 'react';
import { useAuth } from '@/hooks/useAuth';
import LoginButton from '@/components/LoginButton';
import UserProfile from '@/components/UserProfile';
import { TaskSelector } from '@/components/TaskSelector';
import { LogTimeline } from '@/components/LogTimeline';
import { Header } from '@/components/Header';
import { FooterNav } from '@/components/FooterNav';
import { QuickAddButton } from '@/components/QuickAddButton';
import { ManualAddLogModal } from '@/components/ManualAddLogModal';
import { useLogbook } from '@/hooks/useLogbook';

export default function Home() {
  const { user, loading } = useAuth();
  const [isModalOpen, setIsModalOpen] = useState(false);
  const { logs } = useLogbook(); // useLogbookからlogsを取得

  if (loading) {
    return (
      <div className="min-h-screen flex items-center justify-center">
        <p>読み込み中...</p>
      </div>
    );
  }

  return (
    <div className="flex flex-col min-h-screen bg-gray-800">
      <Header />
      <main className="flex-grow w-full pb-16">
        {user ? (
          <>
            <div className="max-w-lg mx-auto p-4 space-y-4">
              <UserProfile />
              <TaskSelector />
              <LogTimeline logs={logs} />
            </div>
            <QuickAddButton onClick={() => setIsModalOpen(true)} />
            <ManualAddLogModal
              isOpen={isModalOpen}
              onClose={() => setIsModalOpen(false)}
            />
          </>
        ) : (
          <div className="min-h-full flex flex-col items-center justify-center">
            <h1 className="text-3xl font-bold mb-8">aina Life アプリ</h1>
            <LoginButton />
          </div>
        )}
      </main>
      <FooterNav />
    </div>
  );
}
```

</details>

### 4.2. 実装のポイント

-   `useLogbook` フックから `logs` を取得し、`LogTimeline` コンポーネントにプロパティとして渡すようにしました。

---

## 5. 履歴画面 (`history/page.tsx`) の実装

### 5.1. `history/page.tsx` の作成

<details>
<summary>クリックしてコードを表示</summary>

```typescript
// src/app/history/page.tsx
'use client';

import React, { useState, useEffect } from 'react';
import { Header } from '@/components/Header';
import { FooterNav } from '@/components/FooterNav';
import { useLogbook } from '@/hooks/useLogbook';
import { LogTimeline } from '@/components/LogTimeline';

const formatDate = (date: Date) => {
  return date.toLocaleDateString('ja-JP', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    weekday: 'short',
  });
};

export default function HistoryPage() {
  const [selectedDate, setSelectedDate] = useState(new Date());

  const goToPreviousDay = () => {
    const newDate = new Date(selectedDate);
    newDate.setDate(newDate.getDate() - 1);
    setSelectedDate(newDate);
  };

  const goToNextDay = () => {
    const newDate = new Date(selectedDate);
    newDate.setDate(newDate.getDate() + 1);
    setSelectedDate(newDate);
  };

  const goToToday = () => {
    setSelectedDate(new Date());
  };

  const { logs, loading } = useLogbook(selectedDate);

  return (
    <div className="flex flex-col min-h-screen bg-gray-800">
      <Header />
      <main className="flex-grow w-full p-4 pb-16">
        <h1 className="text-3xl font-bold mb-4 text-white text-center">履歴</h1>

        <div className="flex items-center justify-center mb-6 space-x-2">
          <button onClick={goToPreviousDay} className="p-2 rounded-full bg-gray-700 text-white hover:bg-gray-600">
            &lt;
          </button>
          <span className="text-xl font-semibold text-white">
            {formatDate(selectedDate)}
          </span>
          <button onClick={goToNextDay} className="p-2 rounded-full bg-gray-700 text-white hover:bg-gray-600">
            &gt;
          </button>
          <button onClick={goToToday} className="ml-4 px-4 py-2 rounded-full bg-blue-600 text-white hover:bg-blue-700 text-sm">
            今日
          </button>
        </div>

        {loading ? (
          <div className="text-center text-white">ログを読み込み中...</div>
        ) : (
          <LogTimeline logs={logs} title={`${formatDate(selectedDate)} の記録`} emptyMessage="この日の記録はありません。" />
        )}
      </main>
      <FooterNav />
    </div>
  );
}
```

</details>

### 5.2. 実装のポイント

-   `selectedDate` ステートで表示する日付を管理し、日付ナビゲーションボタンで変更します。
-   `useLogbook(selectedDate)` を呼び出し、選択された日付のログを取得します。
-   `LogTimeline` コンポーネントに取得したログと、日付に応じたタイトル、空メッセージを渡します。

---

## 6. 動作確認

開発サーバーを起動し、ブラウザでアプリケーションにアクセスして、以下の点を確認してください。

-   `/history` パスにアクセスすると履歴画面が表示されること。
-   日付ナビゲーションボタンで日付を切り替え、表示されるログが変更されること。
-   ログの編集・削除ボタンが機能すること。
